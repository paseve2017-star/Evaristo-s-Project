# NTPRO Software Gyro Compass Repeater — PRD

## Original problem statement
Fullscreen web-based gyro compass repeater displaying live true heading from the Transas NTPRO 5000 simulator. NTPRO's UHI broadcasts NMEA `$HEHDT` over UDP on the LAN; a FastAPI backend listens, parses, and streams heading to a React frontend over WebSocket, rendering a traditional rotating analog compass rose (top-view, full 360°, usable to take bearings in any direction), fixed red lubber line, and a large digital heading readout. Runs fullscreen on a dedicated monitor.

## User choices (locked)
- UDP port: **4001**, bound to **0.0.0.0**
- Dev/testing: **built-in simulator mode** emits synthetic `$HEHDT` to 127.0.0.1:4001 at 10 Hz (SIMULATOR_MODE=true)
- Digital heading format: **tenths** (278.5°) with TRUE label
- MongoDB heading history log: **skipped** (v1)
- Top-view fullscreen rose with bearing-taking at any direction

## Architecture
```
[NTPRO / simulator] --$HEHDT UDP:4001--> [FastAPI :8001]
  asyncio UDP listener -> NMEA parser (checksum-verified) -> WebSocket /api/ws/heading (10 Hz)
                                              + /api/health (last-packet-age)
        ^                                              |
        |________ React frontend (wss, auto-reconnect, rAF smoothing) ___|
```

## Implemented (2026-09-01)
- Backend `/app/backend/server.py`: asyncio UDP listener (0.0.0.0:4001), checksum-verified HDT parser, 10 Hz WebSocket broadcaster (`/api/ws/heading`), `/api/health`, built-in simulator loop with realistic turn dynamics.
- Frontend: SVG 360° compass card (1°/5°/10° ticks, tens labels, cardinals N red-highlighted + intercardinals), unwrapped-angle rAF smoothing (no 360→0 snap), fixed red lubber line, center digital readout (tenths, JetBrains Mono, TRUE label).
- Bearing sight tool: click/drag anywhere on rose; cyan bearing line + TB/RB readouts; reset/toggle buttons.
- Connection LED: green LIVE / amber STALE (>2 s) / red NO DATA, with glow.
- Fullscreen via F key or button; shortcuts F/B/R/S; settings dialog (WS URL override persisted, UDP port, source mode, packet count, last sender); raw NMEA sentence pill in footer.
- Config in `/app/backend/.env`: UDP_PORT, UDP_HOST, SIMULATOR_MODE.

## Test status
Iteration 1 (2026-09-01): backend 6/6 pytest (`/app/backend/tests/test_gyro_repeater.py`), frontend 100%. Parser robustness verified (malformed/bad-checksum rejected, custom valid HDT accepted).

## Backlog (prioritized)
- P0: Deploy to NTPRO LAN PC; create UHI files (`gyroudp.cfg` + `gyroudp.uhs`, timer 100 ms) using NmeaPort.dll pattern; verify `uhiGyroHeading` units/scale; Wireshark/netcat validation.
- P1: Windows packaging (run.bat / service), serve frontend static build from FastAPI for `http://<backend-ip>:8000` access; configurable talker ID filter.
- P2: Rate-of-turn indicator, 60 s heading strip chart, VTG COG/SOG panel, MongoDB history log (24 h TTL), RS-422 physical repeater bridge, multi-station support.

## Session state (2026-09-02: v2 features added)
- App fully working (simulator mode ON). Deploy package complete in /app/deploy/.
- v2 (2026-09-02): ROT indicator (bar ±30°/min, PORT red/STBD green + digital), VTG panel (COG/SOG), 60 s heading strip chart (HeadingChart, unwrapped angles). Backend parses $--ROT/$--VTG (verified_body helper); simulator emits HDT+ROT+VTG at 10 Hz with realistic ±30°/min turns.
- gyrorepeater.uhs now also assigns TEROT + TEVTG; deploy/install-autostart.bat (Task Scheduler, Win7-compatible) added; INSTALL.md gained legacy-Windows section (Python 3.8, Chrome 109/FF ESR 115, or backend on newer PC + Win7 as viewer).
- Tests: 13/13 pass (test_gyro_repeater.py hardened: flood-thread pattern for custom-sentence test, longer poll for drift test; new test_rot_vtg.py).
- KEY FINDING (2026-09-02): user uploaded NmeaPort.dll v5.10.5450; string analysis shows it is SERIAL-ONLY (4800 8N1, "Port = (%s)", GetPrivateProfileSectionA; no UDP/TCP). Integration path changed: NmeaPort writes to a COM port as usual + deploy/ntpro/serial_to_udp.py bridge (pyserial, broadcasts COM lines to 192.168.0.255:4001) + run_bridge.bat on the NTPRO PC.
- Frontend production build created (2026-09-02): /app/frontend/build exists and backend now serves it at / (verified GET / → 200). Single-process deploy ready: export repo → repeater PC needs only backend/, frontend/build/, deploy/.
- PACKAGING FIX (2026-09-03, VERIFIED iteration_3 100%): run.bat cd'd to its own dir (deploy/) so `uvicorn server:app` failed when run from deploy/; changed to `cd /d %~dp0..\backend` so it works in place. Note: user's on-site copy predates this fix — workaround given: copy run.bat INTO backend/ folder, or run uvicorn manually from backend/.
- BUILD REGENERATED (2026-09-03): iteration_4 found frontend/build had vanished from the workspace (exports would have lacked it despite the gitignore fix). Rebuilt via yarn build, backend restarted, GET / → 200 text/html, git check-ignore exit 1 (not ignored). Next Save-to-GitHub export will include the UI build. User restarting run_bridge.bat on-site — reminded that bridge restart does NOT fix their 404; they need the fresh export for frontend/build, and can verify data flow meanwhile via http://localhost:8000/api/health. User's GitHub ZIP was STALE (deploy/offline had only install_offline.bat, no wheels folders) — must re-export. User chose Fix C: pip install --no-index --find-links <own wheels> fastapi uvicorn websockets python-dotenv pyserial. RISK: their wheels were downloaded for the internet PC's Python; if not 3.8, pydantic_core/websockets fail with "not a supported wheel" → then Fix B (pip download --platform win_amd64 --python-version 3.8 --only-binary=:all:) or re-export for bundles.

## Next tasks
1. ON-SITE ISSUE (2026-09-02): repeater PC is OFFLINE (no internet) so pip install failed. SOLVED: prebuilt offline wheel bundles committed at deploy/offline/wheels-py38 (Python 3.8, Win7/2008) and wheels-py312 (Python 3.10-3.12, Win10/11), 15 wheels each ~3.3MB + pyserial added to both (for serial_to_udp.py bridge on the NTPRO PC), plus deploy/offline/install_offline.bat; requirements-repeater.txt now minimal (fastapi, uvicorn, websockets, python-dotenv). User re-exports via GitHub to fetch the bundles.
2. NTPRO-side files authored (2026-09-01): deploy/ntpro/gyrorepeater.uhs (timer 100 ms, TEHDT only, mirrors OverheadDef.uhs pattern — CONFIRMED against user's full OverheadDef.uhs paste; overhead runs at 200 ms/5 Hz, repeater uses 100 ms/10 Hz) + gyroudp.cfg (broadcast 192.168.0.255:4001; [Plugins] block to be copied from user's working NmeaPort.dll config) + INSTALL.md (browser-first verification, scale check, troubleshooting).
2. On-site: user fills gyroudp.cfg [Plugins] from their working NmeaPort config, runs firewall-setup.bat + run.bat on repeater PC, verifies via status LED (green, no SIM badge) and heading scale check.
3. Optional: Windows autostart via shell:startup or NSSM; unicast fallback if broadcast is blocked.

- ON-SITE (2026-09-03, late): user confirmed their local copy has NO frontend/build folder (stale ZIP). server.py updated: when frontend/build is missing at startup, GET / returns a friendly JSON (expected path + fix) instead of cryptic "detail: Not Found". USER ACTION PENDING: Save to GitHub again, download fresh ZIP, verify it contains frontend/build/index.html BEFORE copying, place frontend/build NEXT TO backend/ (C:\gyro-repeater\frontend\build), restart run.bat. Interim data check: http://localhost:8000/api/health.
- User re-exported and confirmed the fresh ZIP contains frontend/build + wheels — downloading now. Next expected: compass UI loads at localhost:8000, then two-PC LAN link.
- ON-SITE (2026-09-03, later): UI now displays on Win2008 Chrome ✓ but LED red, "waiting for $HEHDT" — zero packets reaching backend. serial_to_udp.py upgraded with 2s status prints (forwarded count + last sentence, or "port SILENT" hint) so user can see if NmeaPort is emitting. Diagnostics given: check bridge console for OK/SILENT lines, try TARGET=127.0.0.1 for same-PC, verify NmeaPort link running + COM pair direction, check /api/health packets count.
- ON-SITE (2026-09-03, night): bridge reports "port SILENT" → NmeaPort side fault. PRIME SUSPECT: gyroudp.cfg [Plugins] section is still EMPTY (only comments — user never supplied the working NmeaPort plugin block), so no plugin emits to COM3. Created deploy/ntpro/com_test_writer.py (writes $HEHDT to COM3 at 1 Hz) to isolate com0com pair vs NmeaPort. Asked user for: the working NMEA plugin config (ini/cfg NmeaPort reads — DLL uses GetPrivateProfileSectionA so an .ini likely exists near Nmea.exe) and whether a NmeaPort window appears for the working output.
- User went to run com_test_writer.py isolation test on-site. Next contact: either "repeater showed 123.4° green" (→ fix gyroudp.cfg [Plugins] with their working NmeaPort config — user to paste ini/cfg containing NmeaPort lines) or "still silent with test writer" (→ com0com pair fix).
- User re-pasted OVERHEAD.CFG: overhead link uses NaviSensor.HostApp="OVH_3_FIX_DIM.cne" — output/serial config must live inside the .cne host-app file, not the .cfg. Asked user to paste OVH_3_FIX_DIM.cne contents + any .ini next to Nmea.exe + com_test_writer.py result.
- FILES ANALYZED (2026-09-04): user uploaded OVH_3_FIX_DIM.CNE + OVH_3_FIX_DIM_PS.CNE (binary OLE compound docs w/ obfuscated VBScript — not readable, they're the NaviSensor host apps) + Nmea.ini (text, 6461 bytes: NMEA parameter tables GyroHeading/GyroRateOfTurn/LogWT etc. + [Groups] section — likely read by NmeaPort.dll via GetPrivateProfileSectionA). KEY ON-SITE RESULT: bridge prints "OK forwarded" (com_test_writer proved pair+bridge work) but repeater still red → broadcast 192.168.0.255 does NOT loop back on same PC → fix TARGET=("127.0.0.1",4001) for same-PC test, or repeater-PC IP for two-PC.
- Nmea.ini DECODED (2026-09-04): NmeaPort.dll sentence config. [Gyro] group @Period=1000 emits $HEHDT,%.1f,T (GyroHeading) + $HEROT,%.1f,A (GyroRateOfTurn); separate [GyroRep] group emits $HEHDT from GyroRepHeading (dedicated repeater heading parameter exists!); $GPVTG,%.1f,T,,,%.2f,N,,,%c (COG/SOG knots after 1.944 factor) — all match backend parser field positions ✓. NOTE: groups emit at @Period=1000 (1 Hz), not 10 Hz — .uhs timer does not control NMEA rate; faster rate would need a group with @Period=100 in Nmea.ini (pending confirmation of group-selection mechanism, likely via .cne host app). OPEN: which host-app .cne drives NmeaPort output (overhead uses NaviSensor+OVH_3_FIX_DIM.cne); asked user for UHI folder listing (*.cne/*.ini/*.cfg/*.uhs) and whether OK-forwarding was seen with com_test_writer or real Nmea.exe.
- User's server IPv4: 192.168.0.10 (subnet confirmed 192.168.0.x → broadcast 192.168.0.255). Advised: keep TARGET=127.0.0.1 while bridge+backend are same-PC; for two-PC setup use unicast to the REPEATER PC's IP (need it) or 192.168.0.255. Awaiting: green/red result and which machine 192.168.0.10 is (NTPRO PC vs repeater PC).
- Network cleared up (2026-09-04): NTPRO PC (Win2008 instructor station) = 192.168.0.1; repeater PC = 192.168.0.10; LAN restricted (~up to .200, broadcast unreliable). DECISION: unicast — serial_to_udp.py default TARGET changed to ("192.168.0.10", 4001) with commented examples for 127.0.0.1/broadcast. Still awaiting: green/red result on localhost and UHI folder listing.
- ON-SITE (2026-09-04): user ran bridge with COM_PORT=COM3 + 127.0.0.1 → "port SILENT". Two issues: (a) bridge must read the OTHER pair end (COM4), not COM3 — NmeaPort/writer writes COM3; (b) silence expected anyway since nothing was writing (writer not running, NmeaPort not yet configured). Fixed serial_to_udp.py default to COM4 with explicit pair-direction comments. Next: bridge(COM4)+writer(COM3) test → green 123.4, then UHI folder listing to write gyroudp.cfg [Plugins].
- ON-SITE (2026-09-04): run.bat console showed only `GET /favicon.ico 404` — page itself now LOADS on-site (StaticFiles mount works, earlier 404 resolved). Favicon was missing because CRA template public/ has no favicon.ico. Fix: generated compass-needle favicon.ico into frontend/public + rebuild so exports include it. Cosmetic only.
- Favicon fix built & self-verified (2026-09-04): build/favicon.ico exists (1311 bytes), backend restarted, GET /favicon.ico → 200 image/x-icon, GET / → 200. User's on-site copy needs the next fresh export to include it (cosmetic only — their page already loads and works without it).
- ON-SITE (2026-09-04): same-PC test on 192.168.0.10 with bridge COM4 + 127.0.0.1 → still red "No data". Bridge opens COM4 fine (no serial error) but likely silent because nothing is writing COM3 (user may not have com_test_writer running simultaneously) OR the com0com pair isn't actually COM3↔COM4 (default names are CNCA0/CNCB0 until renamed — a real physical COM4 would also open silently). Diagnostics given: run writer simultaneously, watch for "Access denied" (port held by something else), verify pair via com0com setupc list / Device Manager, check /api/health packets count.
- ROOT CAUSE FOUND (2026-09-04): bridge forwards OK ("$HEHDT,232.4") but page red NO DATA = WebSocket never connected, because the CRA build bakes REACT_APP_BACKEND_URL (cloud preview URL) into the bundle — offline LAN browsers can't reach it. FIX: useHeadingStream defaultWsUrl() now derives from window.location (same-origin), SettingsDialog API now relative "/api". Interim workaround for user's current copy: settings gear → WS URL override → ws://localhost:8000/api/ws/heading → Apply (persisted in localStorage).
- Same-origin fix built & self-verified (2026-09-04): grep shows zero REACT_APP_BACKEND_URL refs, yarn build OK, backend restarted, GET / → 200, WS via preview wss URL streams heading/rot fine. Iteration 8 testing_agent dispatched for confirmation. User told: instant workaround on current copy = settings gear → ws://localhost:8000/api/ws/heading → Apply; permanent fix arrives with next fresh export.
- MILESTONE (2026-09-04): same-PC test GREEN LIVE with com_test_writer data (WS override workaround used). Full chain proven: com0com pair (COM3→COM4) → bridge → UDP 127.0.0.1:4001 → backend → browser. REMAINING: real NmeaPort feed — need UHI folder listing (dir /b *.cne *.ini *.cfg *.uhs) to identify the NMEA host-app .cne for gyroudp.cfg [Plugins] block (pattern from OVERHEAD.CFG: `<Plugin>.HostApp = "<file>.cne"`; overhead uses NaviSensor+OVH_3_FIX_DIM.cne; NMEA output presumably NmeaPort + some .cne).
- UHI folder listing (2026-09-04): only OVH_3_FIX_DIM(_PS).CNE exist — NO NMEA .cne. Conclusion: NmeaPort.dll's HostApp parameter is Nmea.ini itself (DLL reads it via GetPrivateProfileSection). gyroudp.cfg updated with candidate line `NmeaPort.HostApp = "Nmea.ini"`. Asked user to paste UHI.CFG + NAVHELM.CFG (may contain the real NmeaPort plugin line / COM port assignment) and HWGROUP.CFG if needed.
- navhelms.cfg analyzed: only helm button/lamp mappings — dead end for NMEA. NaviSensor.dll strings: COM ProgID "NaviSensor.HostApp" = the .cne host class (so plugin lines are `<ProgID> = <hostapp file>`). NmeaPort.dll ProgIDs are "NaviTrainer.NmeaPort"/"NaviTrainer.NmeaPortTransport" — plugin line key may need to be one of those rather than "NmeaPort.HostApp". Digging for exact key format; asked user for ntpro\hardware folder listing + how the working NMEA output (Nmea.exe) is launched.
- HARDWARE FOLDER (2026-09-04): user uploaded rar but ALL FILES 0 BYTES (archive lost contents — need re-pack or text paste). Listing reveals: NmeaParser.dll, Nmea.ini (co-located with NmeaPort.dll ✓), UhiTransports.dll (transports = plugins, may contain UDP!), many UhiPlugin*.dll, NO Nmea.exe in this folder (it's elsewhere — ask where/how launched). User already copied gyrorepeater.uhs + gyroudp.cfg into the folder ✓ (dated Sep 3/8). Need: `type uhi.cfg` + `type hwgroup.cfg` pasted as TEXT (bypasses broken archive), Nmea.exe location/launch method, and possibly strings of UhiTransports.dll for UDP support.
- Nmea.exe/NmeaParser.dll ANALYSIS (2026-09-04): Nmea.exe is the NMEA/AIS console host app (looks for \nmea.ini, "Fail to create NmeaPort"). NmeaParser.dll instantiates TRANSPORTS by ProgID + config file ("Failed to create transport with progId", "NaviTrainer.NmeaPortTransport" = serial transport). KEY LEAD: if a UDP transport ProgID exists (check UhiTransports.dll — was 0 bytes in rar, need direct upload), Nmea.ini could define UDP output directly → no com0com/bridge needed. Checking full Nmea.ini for TRANSPORT sections.
- Nmea.ini has NO [TRANSPORT] section (only sentence groups/formats) → transport config lives elsewhere (likely the .cfg link file itself, e.g. `NaviTrainer.NmeaPortTransport = "<config>"` mirroring the NaviSensor.HostApp pattern). Digging NmeaParser.dll for config key names (TRANSPORT string found). STILL NEED from user: (1) UhiTransports.dll direct upload (was 0-byte in rar) — to discover ALL transport ProgIDs incl. possible UDP; (2) how Nmea.exe is launched (shortcut Target / command line — it takes a 'param'); (3) `type uhi.cfg` pasted as text.
- TRANSPORTS DECODED (2026-09-04): UhiTransports.dll registers 4 ProgIDs: ComPortTransport (serial), CommuxTransport (mux), DebugUhiTransport, TelnetTransport (TCP sockets - client+server classes, full Winsock). NO UDP transport. NEW PLAN A: Nmea.exe output via NaviTrainer.TelnetTransport over TCP directly to repeater PC — no com0com/bridge. Backend updated: asyncio TCP listener on 0.0.0.0:4002 (env NMEA_TCP_PORT) feeding same handle_sentence(); UDP path unchanged. STILL NEED: TelnetTransport config file format (digging strings; else derive from [CONFIG] TRANSPORT key format in their master config — user to paste uhi.cfg text).
- TCP FEATURE VERIFIED (2026-09-04): iteration_9 → 20/20 pass (new test_tcp_nmea.py: connectivity, valid HDT via WS, checksum rejection, multi-sentence packet, split-packet buffering, UDP/WS regression). Hardening applied: 64KB buffer cap in handle_tcp_client. firewall-setup.bat now also opens TCP 4002. Backend self-test: TCP flood verified heading=45.6 with TCP sender. AWAITING user: uhi.cfg text paste (for [CONFIG] TRANSPORT format) to write the TelnetTransport wiring for direct TCP Plan A.
- UHI.CFG pasted (2026-09-04): it's for physical HID controllers (VRConsole/joystick; [Plugins] EMPTY, [Variables] calibrate() mappings). NOT the NMEA path. Working NMEA/sensor path = OVERHEAD.CFG (NaviSensor.HostApp=OVH_3_FIX_DIM.cne). Missing link: how TEHDT assigns in .uhs reach a physical serial port — suspects: hwgroup.cfg (hardware group→port mapping; [SerialToAddress] Script_ID=Physical_ID in link cfgs relates to MBSEN |1/|2/|3!) and/or the .cne host apps (binary, can't author). Nmea.exe launch method still unknown (takes a 'param'). Researching Transas UHI/NmeaPort config syntax via web search.
- BREAKTHROUGH via NTPRO docs (2026-09-04): NMEA 0183 output is a BUILT-IN feature configured in the Instructor Station's Configuration Editor — add an NMEA interface (e.g. NMEA_LOG_GYRO_ARPA), map it to a COM port, File/Configuration/Save As + set default. Outputs standard $HEHDT/$HEROT via NmeaPort serial. NEW SIMPLEST PATH: Config Editor → NMEA interface → COM3 → existing proven bridge (COM4→UDP) → repeater. Custom gyroudp.cfg/gyrorepeater.uhs becomes PLAN B (UHI transport syntax remains undocumented). Save-as-default step is required for persistence.
- User re-pasted OVERHEAD.CFG (3rd time, as "the working UHI cfg"). Nothing new: NaviSensor.HostApp=OVH_3_FIX_DIM.cne only; [SerialToAddress] empty. STILL MISSING: hwgroup.cfg contents (physical port mapping — likely where Script_ID/MBSEN → COM port routing lives) and whether user tried the Instructor Station Configuration Editor NMEA interface (Plan A from NTPRO docs, recommended).
- PLAN A IN PROGRESS (2026-09-04): user found the Configuration Editor NMEA interface ✓ (was defaulted to COM2), reassigned to COM3 (com0com write end; bridge reads COM4, TARGET 127.0.0.1 for same-PC test). Starting simulator now. Expected: bridge prints "OK forwarded ... $HEHDT,<real heading>" at ~1 Hz, page green LIVE tracking instructor gyro. If green: final step = move backend to repeater PC 192.168.0.10 + TARGET unicast. If silent: check interface saved as default config, sim actually running, bridge on COM4.
- 🎉 LIVE SUCCESS (2026-09-04): built-in NMEA interface (Configuration Editor, COM3) → com0com → bridge → repeater display GREEN, synchronized with real gyro! User then requested SIMPLIFIED UI: removed COG/SOG/VTG panel, ROT indicator and 60s heading chart (kept in repo components, just unmounted); digital readout shrunk (6vmin/3.2vmin); bearing tool kept as-is. Needs rebuild + fresh export for on-site.
- UI SIMPLIFICATION CONFIRMED BY USER (2026-09-04): "just like the sample" ✓. Final display: full-screen compass card only + bearing tool + smaller digital readout (no COG/SOG/ROT/chart). Components RotIndicator/HeadingChart remain in repo but unmounted (backend still parses ROT/VTG — re-mounting restores them). Mobile 390px: header flex-wrap fix + compass max-w-full constraint. Build regenerated for export. USER ACTION: fresh GitHub export → copy frontend/build → restart run.bat on-site.
- 800x800 VERIFIED (2026-09-04): screenshot at exactly 800x800 — compass card 687x687 centered, header/footer fit, LED live, no meaningful overflow. No code change needed. On-site: F key (in-app fullscreen) or F11; recommended kiosk mode: chrome.exe --kiosk http://localhost:8000 for a dedicated monitor. User's target display is 800x800.
- User paused for lunch (2026-09-04). State: live gyro feed working on same-PC test (192.168.0.10 is actually the test machine), simplified UI approved, 800x800 verified. Pending when back: (1) fresh GitHub export with simplified build → replace frontend/build on-site; (2) final two-PC move (backend on repeater PC, bridge TARGET=192.168.0.10, firewall bat); (3) optional kiosk shortcut.
- UI VARIANT (2026-09-04): user requested PLAIN mode — pure analog gyro card only, no digital readout, no bearing, no bars/settings. Implemented via URL param: default URL = plain card only (header/footer/readout/bearing hidden); ?panel=1 restores the full diagnostic display. Keyboard shortcuts (F fullscreen, S settings) still work in plain mode; settings dialog mount kept for on-site WS override via S key.
- PLAIN MODE VERIFIED (2026-09-04): default URL at 800x800 → pure compass card only (header/footer/readout/bearing all absent, compass svg + lubber present) ✓; ?panel=1 at 1920x800 → full diagnostic display intact (LED live, readout, bearing, footer) ✓. Build regenerated — ready for user's next GitHub export.
- FINAL PROCEDURE DOC (2026-09-04): created /app/deploy/SETUP-TWO-PC.md — complete two-PC procedure (NTPRO PC 192.168.0.1: com0com pair COM3/COM4 + Configuration Editor NMEA interface→COM3 + bridge COM4→TARGET 192.168.0.10; Repeater PC 192.168.0.10: offline install, firewall, autostart, plain display + ?panel=1 diagnostics, kiosk shortcut; verification table + troubleshooting). User to source a station PC and run it.
- NEW TOPIC (2026-09-04): user wants USB joystick via UHI (forgot their previous config). Their own UHI.CFG already contains the pattern: [Default] vrconsole.uhs (trace_hid.uhs commented = discovery tool), [Variables] hidRudder/hidTeleL/hidTeleR = calibrate(ain\unit#1\port#N, ...) for analog axes, commented hidBtn1-12 = din\unit#1\port#N for buttons, [HardwareGroups] hwgroup.cfg. Building a refresher guide from this + docs.
- JOYSTICK GUIDE DELIVERED (2026-09-04): created deploy/ntpro/JOYSTICK-UHI-GUIDE.md (full refresher: joy.cpl check → trace_hid.uhs discovery → ain/din port mapping → calibrate() reuse) + joystick-template.cfg. Based on user's own working uhi.cfg pattern (ain\unit#1\port#N analog + calibrate, din\unit#1\port#N buttons, unit#N = HID device index).
- Joystick purpose confirmed: steering (rudder ±35°) + twin telegraph (±10) control of own-ship in NTPRO — matches user's old hidRudder/hidTeleL/hidTeleR mapping exactly. vrconsole.uhs holds the hid→uhi assignments (not yet seen; offered to verify if user pastes it).
- VRCONSOLE.UHS VERIFIED (2026-09-04): complete working control script. Telegraph: hidTeleL/R → uhiEngOrderHandle:0/:1 ✓. Rudder FU mode: changed(hidRudder) AND uhiStrModeSwitch==1 → uhiStrRudderOrder (CRITICAL GOTCHA: stick only steers in follow-up mode!); NFU via din port#9/#10 jog buttons at 100ms. Emerg stop din 7/8; thrusters din 2,4,5,11; console pages din 16-22; viewpoint din 11 + ain 3/9. QUIRKS FLAGGED: din port#11 double-assigned (thruster:1 up AND viewpoint=0); viewpoint condition reads ain port#3 but maps ain port#9 (possible legacy typo). Buttons need NO [Variables] lines — script addresses din ports directly. Only 3 calibrate() lines needed in cfg.
- User taking a break (2026-09-04). State: gyro repeater LIVE on-site via built-in NMEA interface + com0com bridge; plain-mode UI approved (default) with ?panel=1 diagnostics; SETUP-TWO-PC.md procedure delivered; joystick steering/telegraph recipe verified (vrconsole.uhs analysis, FU-mode gotcha documented). When back: fresh export with plain build, two-PC station move, joystick trace/test.
- BOOT AUTOSTART (2026-09-04): user wants zero-touch boot → fullscreen display. Solution: backend autostart via install-autostart.bat (Task Scheduler) + NEW deploy/start-display.bat (waits 10s, launches Chrome --kiosk http://localhost:8000) placed as shortcut in shell:startup. Chrome kiosk supported even on old Win2008 Chrome (unlike Firefox ESR52).
- User away (2026-09-04). Delivered: start-display.bat (kiosk autolaunch via shell:startup) + full boot-autostart procedure. Pending when back: on-site test of boot→display chain, two-PC station move, optional joystick test (vrconsole.uhs recipe). All deploy docs current in /app/deploy (SETUP-TWO-PC.md, JOYSTICK-UHI-GUIDE.md, INSTALL.md).
- IP CORRECTION (2026-09-04): NTPRO PC = 192.168.0.10 (not .0.1 as first stated), Repeater PC = 192.168.0.20. Updated serial_to_udp.py default TARGET → ("192.168.0.20", 4001), gyroudp.cfg comment, SETUP-TWO-PC.md all refs. Firewall/autostart/display steps run on 192.168.0.20; browser URL http://192.168.0.20:8000 (or localhost on that PC).
- User on break (2026-09-04, late). Final on-site state to resume: two PCs connected (NTPRO .10, repeater .20), all IPs corrected in repo files, startup order given (firewall→run.bat on .20; bridge→sim on .10; verify bridge console + /api/health). Expect next contact = two-PC live result.
- ON-SITE ISSUE (2026-09-08): Repeater PC (Win7, Python 3.8) — install_offline.bat wheels-py38 fails with "from versions: none" = find-links folder empty/missing on their copy (stale export again or folder not copied; repo bundle intact: 16 wheels ~3.3MB). Fix offered: verify `dir wheels-py38`, re-export fresh ZIP, OR reuse the self-downloaded wheels that already worked on the NTPRO PC (pip install --no-index --find-links <folder> fastapi uvicorn websockets python-dotenv).
- ON-SITE (2026-09-08): Fix A failed — user's self-downloaded wheels contain fastapi requiring Python >=3.10 (downloaded with the internet PC's modern Python; newest fastapi dropped 3.8). My repo wheels-py38 bundle was built with --python-version 3.8 pin so it resolves compatible versions (fastapi 0.124.4). Guidance: fresh GitHub export for the py38 bundle, OR re-download on internet PC with `pip download ... --only-binary=:all: --platform win_amd64 --python-version 3.8 --implementation cp`.
- EXPORT ISSUE (2026-09-08): user's fresh GitHub export STILL lacks deploy/offline wheels (and possibly build). Suspected cause: files untracked/uncommitted in workspace git → Save-to-GitHub pushes only committed state. Committing everything locally now. Fallback for user (no GitHub needed): internet PC runs `pip download fastapi uvicorn websockets python-dotenv pyserial -d wheels38 --only-binary=:all: --platform win_amd64 --python-version 3.8 --implementation cp` then offline install on Win7 machine.
- WHEELS GAP (2026-09-08): offline install on Win7/py3.8 failed on colorama (Windows-only dep of click; Linux pip download skipped it due to platform markers). Added colorama wheel to both bundles + committed. On-site fix without re-export: internet PC runs `pip download colorama -d wheels38` (pure python), copy over, re-run install command — it resumes and only adds what's missing.
- ON-SITE (2026-09-08): user's manual pip download grabbed starlette 0.51.0 (requires py>=3.10) — they ran the download WITHOUT the --python-version 3.8 pinning flags, so wheels38 folder has mixed incompatible versions. Repo bundle is correct (starlette 0.44.0, committed). Options given: (1) fresh GitHub export (now that bundles are committed — verify on github.com web first), or (2) delete wheels38 entirely and re-run the FULL pinned command: pip download fastapi uvicorn websockets python-dotenv pyserial -d wheels38 --only-binary=:all: --platform win_amd64 --python-version 3.8 --implementation cp, or surgical: delete starlette-0.51.0 whl + download "starlette==0.44.0" with same flags.
- INSTALL SUCCESS (2026-09-08): dependencies installed on Repeater PC (Win7, py3.8) with no errors. Next: firewall-setup.bat (admin) → run.bat → bridge TARGET=192.168.0.20 on NTPRO PC → browser localhost:8000 → verify green card. Then optional autostart pair (install-autostart.bat + start-display.bat in shell:startup).
- 🎉🎉 FULL TWO-PC SUCCESS (2026-09-08): Repeater PC (Win7, py3.8, 192.168.0.20) displays live gyro at localhost:8000, fed from NTPRO PC (192.168.0.10) via built-in NMEA interface → COM3 → com0com → COM4 bridge → UDP unicast 4001 → backend → plain fullscreen card. COMPLETE CHAIN OPERATIONAL ON-SITE. Tomorrow/remaining: optional autostart (install-autostart.bat + start-display.bat shell:startup), fresh export for plain build if not yet applied, optional joystick test.
- ON-SITE (2026-09-09): two-station run fails at bridge — "could not open port COM4: FileNotFoundError". COM4 doesn't exist on THIS NTPRO PC (com0com pair was created on the earlier test machine; this PC lacks it or pair still named CNCA0/CNCB0). Created deploy/ntpro/list_com_ports.py for diagnosis. Guidance: run list_com_ports.py → if pair missing: com0com setupc (list / change PortName=COM3,COM4 / install PortName=COM3 PortName=COM4) → restart bridge. com0com + bridge must both live on the NTPRO PC (.10).
- User also installed com0com on the Repeater PC — advised: harmless, no uninstall needed (repeater uses UDP only; serial pair only matters on NTPRO PC). Optional cleanup if desired.
- ON-SITE (2026-09-09): com0com pair shows in Device Manager (COM3/COM4) but pyserial list_ports shows only COM1/COM2 (physical) and opening COM4 → FileNotFoundError. Note: list_ports missing com0com ports is a KNOWN cosmetic quirk — the real test is opening. Likely causes: needs REBOOT after com0com install/rename (most common), or unsigned-driver issue on Win2008 x64, or pair needs remove+reinstall. Guidance given: reboot → direct open test `python -c "import serial; serial.Serial('COM4',4800)"` → if still failing, check DM yellow warnings / reinstall pair via setupc. ESCAPE HATCH: physical null-modem cable COM1↔COM2 (both real ports visible to pyserial) — NMEA interface → COM1, bridge reads COM2.
- RESUME (2026-09-09): single-PC test on NTPRO PC still solid (COM3 NMEA → COM4 bridge, TARGET=own IP works). TWO-PC now: user configured Repeater PC in NTconfig as additional Host Station assigned as NMEA station — enables OPTION A: NMEA output runs ON the repeater station (data rides NTPRO's own LAN protocol, no UDP/firewall across LAN): com0com COM3→COM4 + serial_to_udp.py (TARGET=127.0.0.1) + backend ALL on the Repeater PC. OPTION B (proven fallback): NTPRO PC bridge TARGET=("192.168.0.20",4001) unicast → repeater backend (needs firewall rule on .20). com0com already installed on repeater PC (useful for Option A after all!).
- OPTION A LIVE ON-SITE (2026-09-09): two-PC setup fully working via NMEA station architecture. Two new requests: (1) display modifiability → added URL query params (no rebuild needed): default plain card; ?panel=1 full; ?digital=, ?bearing=, ?hud= individual on/off toggles, combinable. Rebuild + export needed once, then all display changes are URL-only. (2) joystick detection for rudder/engine → created deploy/ntpro/joystick-detect.cfg (trace_hid.uhs discovery link) + existing JOYSTICK-UHI-GUIDE.md + verified vrconsole.uhs mapping (hidRudder/hidTeleL/hidTeleR, FU mode gotcha).
- FINAL AS-BUILT DOC (2026-09-09): deploy/FINAL-SETUP.md — complete verified procedure of the ACTUAL working setup (Option A station architecture: NTPRO PC .10 sim-only; repeater .20 = NMEA station + com0com + bridge TARGET 127.0.0.1 + backend + kiosk display). Includes one-time setup, daily startup order, URL display-mode table, verification + troubleshooting. URL param switches built & deployed (rebuilt, backend restarted; screenshot verification dispatched).