; ============================================================
; JOYSTICK DETECTION link file - run this FIRST to discover
; which port#N each joystick axis/button reports on.
;
; HOW TO USE:
;   1. joy.cpl  -> confirm Windows sees the joystick
;   2. Register/start THIS cfg as a UHI link (same way as the others)
;   3. Move each axis / press each button - the trace output shows
;      the ain/din addresses (e.g. ain\unit#1\port#4)
;   4. Write them down, then put them into your real link cfg
;      (see joystick-template.cfg) and switch [Default] back to
;      vrconsole.uhs
; ============================================================

[SerialToAddress]
;Script_ID=Physical_ID

[Calibration]

[Plugins]

[Variables]

[HardwareGroups]
hwgroup.cfg

[Default]
trace_hid.uhs
