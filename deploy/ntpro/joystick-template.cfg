; ============================================================
; UHI link file TEMPLATE - USB joystick controller
; Copy to your UHI folder, register in uhi.cfg like the others.
; Fill port#N values from a trace_hid.uhs discovery run.
; ============================================================

[SerialToAddress]
;Script_ID=Physical_ID

[Calibration]

[Plugins]

[Variables]
; --- Analog axes (edit port# after tracing) ---
; hidRudder = calibrate(ain\unit#1\port#4, 0, -35, 2100, 0, 2300, 0, 4095, +35)
; hidTeleL  = calibrate(ain\unit#1\port#6, 0, -10, 2100, 0, 2200, 0, 4095, +10)
; hidTeleR  = calibrate(ain\unit#1\port#7, 0, -10, 2100, 0, 2200, 0, 4095, +10)

; --- Buttons (digital in) ---
; hidBtn1 = din\unit#1\port#1
; hidBtn2 = din\unit#1\port#2
; hidBtn3 = din\unit#1\port#3
; hidBtn4 = din\unit#1\port#4

[HardwareGroups]
hwgroup.cfg

; --- Discovery mode: run with trace_hid first to find port numbers ---
; [Default]
; trace_hid.uhs

; --- Normal mode ---
[Default]
vrconsole.uhs
